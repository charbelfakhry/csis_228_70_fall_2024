<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <title>Index Page</title>
</head>
<body>
    <div class="container">
        <h1>Hello <span style="color: green"><%= username %></span></h1>
        <h3>Your daily message is: <span style="color: #ff0000"><%= message %></span></h3>
        <p>The content of the message: <%= content %></p>
        <div class="row">
            <div class="col">
                <label class="" style="font-weight: bold;">Username: </label>
                <input type="text" />
            </div>
        </div>
        <div class="row">
            <div class="col">
                <label style="font-weight: bold;">Password: </label>
                <input type="password" />
            </div>
        </div>
        <div class="row">
            <div class="col-2">
            </div>
            <div class="col">
                <button class="btn btn-success btn-sm text-light">Login</button>
            </div>
        </div>

        <table class="table">
          <thead>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Password</th>
            <th>DOB</th>
            <th>Order#</th>
          </thead>
          <tbdoy>
            <% for(let i = 0; i < users.length; i++){ %>
              <tr>
                <td><%= users[i].user_id %></td>
                <td><%= users[i].client_username %></td>
                <td><%= users[i].client_email %></td>
                <td><%= users[i].client_password %></td>
                <!-- <td><%= users[i].user_dob %></td> -->
                <td><%= users[i].client_dob?.toLocaleDateString() %></td>
                <td>
                  <button type="button" class="btn btn-info">
                    Notifications <span class="badge badge-light"><%= users[i].order_count %></span>
                  </button>
                </td>
              </tr>
            <% } %>
          </tbdoy>
        </table>
    </div>
</body>
</html>